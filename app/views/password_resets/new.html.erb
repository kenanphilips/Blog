<h1>Forgot Password</h1>

<%= form_for(:password_reset, url: password_resets_path) do |f| %>
  <div>
    <%= f.label :email %><br>
    <%= f.email_field :email %>
  </div><br>
  <%= f.submit "Submit" %>
<% end %>
